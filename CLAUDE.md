@AGENTS.md

## Claude Code

Use the shared EngineeringSpec workflow above. Keep Claude-specific configuration outside the normative specification.

Use `engineeringspec next`, then `engineeringspec work <contract-id>` only when implementation permission is reported. Compact JSON is the default; `--verbose` preserves the full report. Current-change classification is not permission, and `work` never predicts the eventual workflow lane.
