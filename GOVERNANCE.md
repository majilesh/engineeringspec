# Governance

Maintainers approve specification changes. Semantic changes require an RFC and matching conformance changes. Specification and implementation changes are reviewed separately. Vendor-specific agent behavior is not normative without ecosystem justification.

The project will describe itself as a mature open standard only after it has public governance and specification, conformance tests, a reference implementation, an independent implementation, interoperability demonstrations, and adoption feedback.
