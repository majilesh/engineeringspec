# Contributing

Open an issue before large changes. Normative changes use the RFC template, explain compatibility and security impact, and add conformance fixtures. Run `npm run lint`, `npm run typecheck`, `npm test`, `npm run test:conformance`, and `npm run build` before submitting.
