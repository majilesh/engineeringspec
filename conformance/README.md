# Conformance fixtures

Fixtures are standalone inputs for any implementation. `expected-results/manifest.json` declares validity and required stable diagnostic codes; valid canonical JSON outputs live beside future expanded cases.
